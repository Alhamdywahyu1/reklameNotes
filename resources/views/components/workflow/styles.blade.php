<style>
    .workflow-hero {
        background: linear-gradient(135deg, #f8fdff 0%, #e0f2fe 100%);
        border: 1px solid rgba(2, 132, 199, 0.15);
    }

    .workflow-summary-card {
        height: 100%;
    }

    .workflow-summary-card .number {
        font-size: 2rem;
        font-weight: 800;
        line-height: 1;
        margin-bottom: 0.35rem;
        color: #0f172a;
    }

    .workflow-summary-card .label {
        font-size: 0.95rem;
        font-weight: 600;
        color: #475569;
        margin-bottom: 0.3rem;
    }

    .workflow-summary-card .helper {
        font-size: 0.85rem;
        color: #64748b;
        margin: 0;
    }

    .workflow-quick-link {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        padding: 1rem 1.15rem;
        border: 1px solid rgba(2, 132, 199, 0.12);
        border-radius: 1rem;
        text-decoration: none;
        color: inherit;
        background: #fff;
        transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
    }

    .workflow-quick-link:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 24px rgba(2, 132, 199, 0.08);
        border-color: rgba(2, 132, 199, 0.25);
        color: inherit;
    }

    .workflow-quick-link .icon-wrap {
        width: 48px;
        height: 48px;
        border-radius: 14px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        font-size: 1.35rem;
    }

    .workflow-quick-link .link-title {
        font-weight: 700;
        color: #0f172a;
        margin-bottom: 0.15rem;
    }

    .workflow-quick-link .link-text {
        color: #64748b;
        font-size: 0.9rem;
        margin: 0;
    }

    .icon-approval {
        background: rgba(2, 132, 199, 0.1);
        color: #0369a1;
    }

    .icon-print {
        background: rgba(22, 163, 74, 0.12);
        color: #166534;
    }

    .icon-map {
        background: rgba(220, 38, 38, 0.1);
        color: #b91c1c;
    }
</style>
